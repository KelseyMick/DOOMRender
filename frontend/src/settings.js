export const DOOM_RES = { width: 320, height: 200 };

export const SCALE = 5.0;

export const WIDTH = DOOM_RES.width * SCALE;
export const HEIGHT = DOOM_RES.height * SCALE;
export const H_WIDTH = WIDTH / 2;
export const H_HEIGHT = HEIGHT / 2;

export const FOV = 90.0;
export const H_FOV = FOV / 2;

export const PLAYER_SPEED = 0.3;
export const PLAYER_HEIGHT = 41;

// this.FOV = 90.0;
// this.H_FOV = this.FOV / 2;
// this.H_WIDTH = this.canvas.width / 2;
// this.SCREEN_DIST = this.H_WIDTH / Math.tan(this.radiansToDegrees(this.H_FOV));
