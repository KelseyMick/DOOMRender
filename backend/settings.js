const WIDTH = 320;
const HEIGHT = 200

const SCALE = 5.0;
const WINDOW_WIDTH = parseInt(WIDTH * SCALE);
const WINDOW_HEIGHT = parseInt(HEIGHT * SCALE);

const H_WIDTH = WINDOW_WIDTH / 2;
const H_HEIGHT = WINDOW_HEIGHT / 2;

module.exports = {
  H_WIDTH: H_WIDTH,
  H_HEIGHT: H_HEIGHT,
};